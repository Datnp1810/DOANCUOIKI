@model IEnumerable<DOANCUOIKI.Models.HTThanhToan>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Add">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdThanhToan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HinhThuc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdThanhToan)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.HinhThuc)
            </td>
            <td>
                <a asp-action="Update" asp-route-id="@item.IdThanhToan">Edit</a> |
                <a asp-action="Display" asp-route-id="@item.IdThanhToan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdThanhToan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
